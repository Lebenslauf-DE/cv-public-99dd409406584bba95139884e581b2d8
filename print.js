'use strict';let printing=false;async function printCV(){if(printing)return;printing=true;try{await Promise.all([...document.images].map(i=>i.decode().catch(()=>{})));await document.fonts.ready;window.print();}finally{printing=false;}}document.getElementById('print-cv').addEventListener('click',printCV);if(new URLSearchParams(location.search).get('print')==='1')window.addEventListener('load',()=>{history.replaceState(null,'',location.pathname);printCV();},{once:true});